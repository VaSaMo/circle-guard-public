spring:
  flyway:
    enabled: false
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: none
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: password

jwt:
  secret: "test-secret-key-that-is-at-least-32-characters-long-12345678"
